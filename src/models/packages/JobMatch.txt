import Developer from "../Developer";
import Enterprise from "../Enterprise";
import { IMatch } from "./IMatch.ts==s";

export default class JobMatch  {

    private jobCode!: number;
    private skillCode!: number;

    public verifyMatch(developer: IMatch, enteprise: IMatch){
        
    }
}